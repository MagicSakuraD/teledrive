# SafetyChart 轮廓线显示修复

## 修复内容

### 1. 颜色函数错误修复
**问题**：0.5m距离的颜色定义有typo (`##f43f5e` 双重#)
**修复**：改为正确的 `#f43f5e` (玫瑰红色)

### 2. 注释更新
**之前**：注释说"只渲染2m和5m的轮廓线"
**现在**：更新为"渲染所有距离的轮廓线：0.2m, 0.5m, 1m, 2m, 5m"

### 3. 添加调试信息
- 显示原始数据长度
- 显示当前角度和加速度
- 显示每个距离组的点数和颜色信息

### 4. 添加图例
在图表左上角添加了颜色图例，显示：
- 🔴 **0.2m** - 红色 (危险)
- 🌸 **0.5m** - 玫瑰红 (警告)  
- 🟠 **1m** - 橘色 (注意)
- 🟢 **2m** - 浅绿 (安全)
- 🟢 **5m** - 深绿 (安全)

## 技术细节

### 距离过滤逻辑
代码中的 `filteredGroups` 已经包含了所有5个距离的过滤：
```javascript
if ([0.2, 0.5, 1, 2, 5].includes(point.distance)) {
  // 处理该距离的数据点
}
```

### 渲染逻辑
每个距离组只要有2个或以上的数据点就会被渲染为轮廓线：
```javascript
if (group.points.length >= 2) {
  return <Line ... />
}
```

## 调试输出
现在控制台会显示：
```
SafetyChart - 原始数据长度: 150
SafetyChart - currentAngle: -5.2
SafetyChart - currentAcceleration: 0.8
SafetyChart - 处理后的数据组数: 5
距离 0.2m: 10 个点, 颜色: #b91c1c
距离 0.5m: 15 个点, 颜色: #f43f5e
距离 1m: 20 个点, 颜色: #ea580c
距离 2m: 25 个点, 颜色: #84cc16
距离 5m: 30 个点, 颜色: #22c55e
```

## 预期效果

现在的 SafetyChart 应该能正确显示包括 0.5m 在内的所有5条轮廓线，每条线用不同颜色表示不同的安全距离。如果某个距离的数据不足（少于2个点），该轮廓线不会显示，这是正常行为。

## 如果0.5m轮廓线仍然不显示

可能的原因：
1. **数据源问题**：ROS端可能没有发送0.5m距离的数据
2. **数据格式问题**：距离值可能不是精确的0.5，而是0.50001这样的浮点数
3. **数据量不足**：0.5m距离的数据点少于2个

查看控制台调试信息可以帮助诊断具体问题。
